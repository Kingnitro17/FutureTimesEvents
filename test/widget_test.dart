import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Full integration tests require Firebase + API keys.
    // This placeholder ensures the test suite compiles.
    expect(true, isTrue);
  });
}
